/**
 * The maximum Z-index used in certain popups and dropdowns.
 */
export const MAXIMUM_Z_INDEX: string = "1999999999";

/**
 * Reserved cascading class name for buttons that may be navigated
 * through buttons or keyboard keys.
 */
export const BUTTON_NAVIGABLE = "button-navigable";

/**
 * Reserved cascading class name for containers that may be
 * escaped by the `escape` input action.
 */
export const ESCAPABLE = "escapable";

/**
 * Most common transition duration.
 */
export const COMMON_DELAY = 200;

/**
 * Transition duration when opening or closing
 * a live tiles container.
 */
export const TILES_OPEN_DELAY = 200;